import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const poppins = Poppins({
  subsets: ["latin"],
  variable: "--font-poppins",
  weight: ["400", "500", "600", "700"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Sigma Master Security Sdn. Bhd. | Professional Security Services in Malaysia",
  description: "23 years of excellence in security services. Your trusted security partner offering armed guards, K9 units, CMS monitoring, emergency response, and technology integration across Malaysia.",
  keywords: ["security services", "armed guards", "K9 unit", "CMS monitoring", "emergency response team", "security Malaysia"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${poppins.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
